@model IEnumerable<DiagnosticCenterManagement.Data.Models.Patient>

@{
    ViewBag.Title = "Index";
}

<h2>Manage Patients</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UserId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Surname)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.UserId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Surname)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PatientAMKA }) |
            @Html.ActionLink("Details", "Details", new { id=item.PatientAMKA }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PatientAMKA })
        </td>
    </tr>
}

</table>
